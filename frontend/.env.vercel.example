# Plantilla del bloque para pegar en Vercel → tu proyecto → Settings → Environment
# Variables (pega directo en el primer campo "Key" y Vercel lo separa solo). Copia a
# ".env.vercel" (ya está en .gitignore) con los valores reales. Ver DEPLOYMENT.md.

API_URL=https://tu-backend.onrender.com/api
WHATSAPP_SALES_NUMBER=573000000000
